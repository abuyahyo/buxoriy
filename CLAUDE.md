# Buxoriy — Имом Бухорий «Саҳиҳ» тўплами

Имом Бухорийнинг «Ал-Жомиъ ас-Саҳиҳ» ҳадис тўпламининг ўзбекча таржимаси. Статик веб-сайт: https://abuyahyo.github.io/buxoriy/

## Тузилма

- `index.html` — бир-файлли веб-илова (HTML + CSS + JS). Уч босқичли юклаш:
  1. **Инлайн BOOKS** (~10 КБ HTML ичида) — бош саҳифа дарҳол кўрсатилади.
  2. **`index.json`** (~2.5 МБ) — preload билан паралелл юкланади, китоб саҳифалари метамаълумоти.
  3. **`data.json`** (~10.9 МБ) — prefetch билан орқа фонда юкланади, ҳадис матнлари ва қидирув учун.
- `data.json` — барча китоб/боб/ҳадислар (минификация қилинган JSON, индентациясиз). ~10.9 МБ. 7,558 та ҳадис, ҳаммаси таржимали.
- `arabic.json` — ҳар ҳадиснинг тоза арабча иснод+матни (`id` → матн). ~3.7 МБ, **7,558/7,558 ҳадис (100%)** қамрайди. Фақат фойдаланувчи ҳадис саҳифасида **«Арабча»** тугмасини босганда lazy-юкланади (precache эмас, SW fetch орқали кешланади), Amiri/Noto Naskh Arabic шрифтлари билан RTL кўрсатилади. Учта манбадан тўлдирилади: **build_arabic.py** скелет-мослаштириш (асосий) + **fill_missing_arabic.py** `ar_source.json` дан `hadithnumber` бўйича (skeleton чиқариб ташлаганларни қайтаради) + **fill_range_arabic.py** канон range-ҳадислари учун range-leader дан нусха (масалан 5709-5712 диапазонида h5712 ← h5709). `ARABIC_ENABLED` (index.html) тугмани ёқади/ўчиради.
- `index.json` — китоб/боб метамаълумоти ва ҳадис сонлари (ҳадис контенти йўқ). `build_index.py` орқали `data.json` дан генерация қилинади. ~1.6 МБ.
- `sw.js` — Service Worker, офлайн режим ва янгилаш банери учун. `CACHE = 'buxoriy-v<timestamp>'` номини `build_index.py` ҳар сафар ўзгартиради.
- `manifest.json` — PWA манифест (Add to Home Screen учун).
- `apple-touch-icon.png` — iPhone "Add to Home Screen" иконкаси (180×180).
- `favicon` — `index.html` ичида inline SVG.
- `ar/` — **алоҳида тўлиқ арабча сайт** (https://abuyahyo.github.io/buxoriy/ar/). Ўз `index.html` (мустақил кодбаза, event-listener архитектураси, RTL), `data.json` (китоб→ҳадис, боблар йўқ; ҳар ҳадис фақат `id`+`matn`), `index.json`, `sw.js`, `manifest.json` билан. Асосий сайтдан соддароқ: улашиш, изоҳ/ровий/манба, боблар йўқ (арабчада бу маълумотлар умуман йўқ). Бош саҳифа тагида **«Ўзбекча ↗»** footer ҳаволаси орқали асосий сайтга ўтилади (асосий сайтда мос «عربي ↗»). Шрифт: барча матн iOS тизим шрифтида (`-apple-system`/`system-ui` → SF Arabic), Amiri/Cairo/Noto Naskh fallback. **Арабча PDF**: бош саҳифада тўлиқ PDF тугмаси + китобма-китоб «yuklab» саҳифаси (`pgYuk`, `openYuklab()`); файллар асосий `pdf` GitHub release'дан (`buxoriy-arab-toliq.pdf` + `kitob-NN-ar.pdf`), `build_pdf.py --ar --ar-books` билан генерация.

> ⚠️ **МУҲИМ ҚОИДА (фойдаланувчи кўрсатмаси):** функционалда бирон ўзгариш киритилса, уни **иккала саҳифага ҳам** (`index.html` ва `ar/index.html`) қўллаш керак — мос келадиган жойда. Бирини янгилаб, иккинчисини унутмаслик. Агар ўзгариш арабча сайтга мантиқан тегишли бўлмаса (масалан ровий/манба/боблар — арабчада йўқ), буни фойдаланувчига айтиб ўтиш.

GitHub Pages автоматик `main` бранчдан хизмат кўрсатади.

### Манбадан тўлиқ қайта қуриш (`build_from_txt.py`)

`data.json` бутун `sahihul-buxoriy.txt` манба файлидан генерация қилинган:
```bash
python3 build_from_txt.py   # sahihul-buxoriy.txt -> data.json
python3 build_index.py      # index.json + inline BOOKS + sw.js
```

`sahihul-buxoriy.txt` тузилмаси: `N-KITOB:` → `K.B-BOB:` → `[Hadis N]`. Парсер:
- **Фақат ўзбекча** сақланади (арабча оят/иснод ташланади).
- `[N]` footnote'лар (боб доирасида) матн ичига `(...)` шаклида киритилади; узун оят таржималари боб сарлавҳасидан `izoh` га кўчирилади.
- `ИЛОВАЛАР` бўлимидаги тафсилий изоҳлар тегишли ҳадис `izoh` майдонига қўшилади.
- `МУҚАДДИМА` ва `[Kirish]` блоклари ташланади.
- **Китоб id** кетма-кет 1..98 (манбада 86 икки марта рақамланган); **боб id** ҳар китоб ичида 1..N (манбадаги «Такрорий» боблар бир хил рақамга эга).

### Олтин Силсила интеграцияси (`buxoriy kitoblari/*.docx` + Android app)

Шайх Муҳаммад Содиқ Муҳаммад Юсуф бошчилигидаги жамоа таржимаси, икки манбада:

**Манба 1 — DOCX (`buxoriy kitoblari/*.docx`, 97 та файл):**
- Ҳар китоб алоҳида docx; ҳадислар `N.` ёки `N1, N2, N3.` ёки `N1-N2.` ёки `N1-N2-N3.` бошланади.
- **`parse_docx_silsila.py`** уларни ўқиб, `silsila_index.json` (gitignore) генерация қилади: `{id: {rowi, matn, izoh, file, id_group, group_idx, group_total}}`.
- Multi-ID гуруҳ (масалан `408, 409.`) — битта `«...»` блок 2-3 ID орасида тақсимланади, **closure** (`»...дедилар.`) асосида.
- Жуфт ID да биттаси етишмаса, биринчи матн нусхаси қолганларига кўчирилади (парциал нашрнинг канон тузилиши).

**Манба 2 — Android app (`uz.hilolnashr.oltin_silsila`, эмуляторда):**
- DOCX'да йўқ ёки парциал бўлган ҳадислар учун ишлатилади.
- ADB орқали бошқарилади: `batch_search.py` ҳар ID учун қидирув очиб, `_a`/`_b`/`_c` скриншотларни олади.
- App ҳар бир ҳадисни предшественник (id-1) билан жуфтда кўрсатади (`X, Y-ҳадис` сарлавҳаси), уларнинг матни бир хил.
- Шу сабабли **`apply_pair_copies.py`** автомат қилади: каноник предшественнинг матнини ID га нусхалайди.

**Тузатиш скриптлари:**
- `apply_silsila.py` — `silsila_index.json` дан `data.json` га ёзади (фақат `tarjima_status=missing` га).
- `apply_pair_copies.py` — predecessor пайдар-пай гипотезаси (app асосли).
- `apply_app_translations.py` / `update_pilots_to_silsila.py` — қўлда extracted мисолларга.
- `TRANSLATION_STYLE.md` — қўлдан таржима учун услуб қўлланмаси (`«...»` форматти, ровий шакллари, имло, footnote қоидалари).

### Build қадами

**Ҳар сафар `data.json` ўзгартирилгандан кейин:**
```bash
python3 build_index.py
```

Бу скрипт уч ишни бажаради:
- `index.json` ни регенерация қилади (китоб/боб метамаълумоти, `hadislar` ўрнига `hC` майдони).
- `index.html` ичидаги `<script id="books-data">` блокига янги BOOKS JSON ни жойлаштиради (бош саҳифа учун инлайн маълумот: `id, nomi, bC, hC`).
- `sw.js` ичидаги `CACHE` номини `buxoriy-v<data.json-mtime>` га янгилайди — бу фойдаланувчилар браузерида янги SW ни триггер қилади, "🔄 Янги версия мавжуд" банер чиқади.

Иккаласи ҳам сонлар мос бўлиши учун шарт. Build қилмасанг, бош саҳифа эскиради ва фойдаланувчилар янги маълумотни олмайди.

> ⚠️ **ФАҚАТ `index.html` (ёки `ar/index.html`) ўзгартирилса** (фронт-енд код, `data.json` эмас): `build_index.py` кеш версиясини **`data.json` mtime'дан** олгани учун, у ўзгармаса cache id ҳам ўзгармайди → Service Worker эски `index.html` ни кешдан беради, фойдаланувчилар янгиланишни **олмайди**. Шунинг учун бундай ҳолда мос `sw.js` (асосий учун `sw.js`, арабча учун `ar/sw.js`) ичидаги `CACHE = 'buxoriy-v<son>'` ни **қўлда** янги timestamp'га (`date +%s`) bump қилиш ШАРТ.

## data.json структураси

```
[
  {
    "id": 1,
    "nomi": "Ваҳийнинг бошланиши китоби",
    "boblar": [
      {
        "id": 1,
        "nomi": "1-БОБ. ...",
        "muallaqot": "...",       // ихтиёрий
        "izoh": "...",             // ихтиёрий
        "hadislar": [
          {
            "id": 294,
            "matn": "...",          // ҳадис матни
            "rowi": "...",          // ровий
            "izoh": "...",          // ихтиёрий — изоҳ
            "manba": "..."          // ихтиёрий
          }
        ]
      }
    ]
  }
]
```

## Таҳрирлаш услуби

Иш услуби: фойдаланувчи манба (PDF/расм/матн) юборади, мен ҳар бир боб/ҳадис ҳолатини солиштириб, фарқларни `data.json`-да янгилайман.

### Манба ва маълумот солиштириш

- Манбадаги матнни **айнан** кўчираман — имло, тиниш белгилари, сўз тартиби кофинча муҳим.
- Қуръон оятлари, илова бўлим, footnote, изоҳлар сақланади.
- Агар манбада боб номи бўш бўлса (масалан, фақат "X-БОБ"), `nomi` ҳам шундай бўш сақланади.

### Боб номи формати

`data.json` даги `nomi` майдон **доимо** қуйидаги шаклда:
- Сарлавҳа билан: `"N-БОБ. Сарлавҳа матни"` (масалан, `"1-БОБ. ИЙМОН ВА НАБИЙ..."`)
- Сарлавҳасиз: `"N-БОБ"` (нуқтасиз — display функция нуқта қўшади)

Янги боб қўшилганда ёки манбадан кўчирилганда шу форматга риоя қилиш керак. Меъёрсиз вариантлар (`1-боб.`, `4 БОБ.`, `11- БОБ.`) ишлатилмайди.

### Matn формати

- Параграфлар орасида `\n\n` (иккита).
- Айрим ҳолларда битта сатр кўчиш `\n` билан.
- Манбадаги "X. Narrator розияллоҳу анҳодан ривоят қилинади:" киришма ҳадис матнига кирмайди — у `rowi` майдонида сақланади.

### Rowi формати

- Оддий: `"Оиша розияллоҳу анҳо"`
- Занжир: `"Зайнаб — Умму Салама розияллоҳу анҳодан"` (Х уни Y дан ривоят қилади)
- Махсус: `"Умайр (Ибн Аббос розияллоҳу анҳумонинг мавлоси)"`

### Footnote (`*`) ва изоҳ

Манбада ҳадис матни ичида `*` белгиси билан изоҳга ишора қилинса:
- `*` белгиси `matn` ичида қолади (масалан, "хамийла*", "Журуфдаги*")
- Изоҳ матни ҳам `matn` охирига `\n\n* ...` шаклида қўшилади (битта footnote бўлса)
- Алоҳида "Изоҳ:" блоки бўлса, у `izoh` майдонига ёзилади

Мисол (322-ҳадис):
```
matn: "...жунубликда бир идишдан ғусл қилар эдик».\n\n* 298-ҳадисга қаранг."
izoh: "Бу ва бошқа бир неча ҳадисларда «нифос» деб..."
```

### Имло хоссалари

- "розияллоҳу анҳо/анҳу/анҳумо" — нинг шакли мос ровийга қараб ёзилади
- "соллаллоҳу алайҳи васаллам" — Расулуллоҳ номи билан
- "ҳ" (Уzб) ва "х" Кириллда фарқли — манбадаги сингари тутилади
- Қавс билан изоҳ: `(...)` — таржимон қўшимчаси
- Мураббаъ қавс: `[...]` — алтернатив таржима ёки нашрга оид

## JSON форматини сақлаш

`data.json` минификация қилинган (индентациясиз). Янгилашда қуйидаги Python шакли ишлатилади:

```python
with open('data.json', 'r', encoding='utf-8') as f:
    db = json.load(f)
# ... ўзгаришлар ...
with open('data.json', 'w', encoding='utf-8') as f:
    json.dump(db, f, ensure_ascii=False, separators=(',', ':'))
```

`indent=2` ИШЛАТИЛМАСИН — у бутун файлни форматлаб юборади ва диff жуда катта бўлади.

## Код тузилмаси (`index.html`)

Битта файл: `<head>` + inline CSS → 4 та `<div class="page">` блок → `<script>` JS.

### Саҳифалар (4 та)

Ҳамма саҳифа DOM'да доимий туради, бирваракай битта кўриниб туради — `showPage(id)` бошқаради.

- `pgHome` — китоблар грид + қидирув инпут + қидирув натижалари
- `pgBob` — танланган китобнинг боблари
- `pgHadisList` — танланган бобнинг ҳадислари
- `hadisPage` — битта ҳадис тўлиқ матн билан

### Glob state

```js
let DB = [];                  // data.json — массив
let ST = { page, kid, bid, hid };   // жорий навигация
let _searchState = { query, kitob[], bob[], hadis[], hadisShown };
let fs, th;                   // localStorage: фонт ўлчами, тема
```

### Hash routing

URL hash'и саҳифа ҳолатини сақлайди. Browser back/forward `applyHash()` орқали қайта тиклайди.

- `#k/1` — китоб
- `#k/1/b/2` — боб
- `#k/1/b/2/h/3` — ҳадис
- `#q=сўз` — қидирув натижалари (encodeURIComponent)

### App объекти асосий методлари

| Метод | Вазифаси |
|-------|----------|
| `goHome(_keepSearch)` | Бош саҳифа. Флаг билан қидирувни сақлаш мумкин |
| `openKitob(kid, scrollToBid?)` | Китоб бобларини кўрсатиш |
| `openBob(kid, bid, scrollToHid?)` | Бобнинг ҳадисларини кўрсатиш |
| `openHadis(kid, bid, hid)` | Ҳадис тўлиқ кўриниш |
| `doSearch(v, fromHash?)` | Қидирув: 3 та секцияда натижа (китоб/боб/ҳадис) |
| `_renderSearch()` | `_searchState`'дан DOM яратиш |
| `showMoreHadis()` | Кейинги 100 та ҳадисни кўрсатиш |
| `restoreSearch()` | Натижаларга қайтиш |
| `prevHadis()` / `nextHadis()` | Ҳадислар ўртасида ўтиш |
| `toggleTheme()` / `adjustFontSize()` / `copyHadis()` / `toggleIzoh()` | UI |

### Helper функциялар

- `getK(kid)`, `getB(kid,bid)`, `getH(kid,bid,hid)` — массивдан топиш
- `allH()` — ҳамма ҳадислар flat массиви (prev/next учун)
- `bobNomi(s)` — боб номини форматлаш (катта/кичик ҳарф, "Аллоҳ", "Набий" автоматик)
- `snippet(text, query)` — қидирувда сўз атрофида ~200 ҳарф
- `escHtml(s)`, `escRegex(s)` — XSS / regex ҳимояси
- `syncSearchToHash(q)` — қидирувни URL'га чиқариш (push/replace)
- `homeBcItems()` — breadcrumb'нинг бошланиши («Бош саҳифа» + ихтиёрий «"X" қидирув» линки)
- `bc(containerId, items)` — breadcrumb HTML қуриш

### CSS наименование

- `.kitob-*` — китоб карталари (top, num, name, stats)
- `.bob-*` — боб карталари (card, name, izoh, count, muallaqot-badge)
- `.hadis-*` — ҳадислар (item, text, izoh, narrator, source, tb-btn)
- `.result-*` — қидирув натижалари (section, item, label, text, where, more-btn)
- `.bc-*` — breadcrumb (trail, link, sep, cur)
- `.fab-*` — флоат тугмалари (nav, theme)
- `.update-banner` — PWA янги версия огоҳлантириши
- CSS variables `:root` ва `.light` — тема рангларини бошқаради
- Тўрт хил картага `content-visibility:auto` + `contain-intrinsic-size` қўшилган — кўринмаган карталар рендер қилинмайди (катта рўйхатларда тезкорлик).

## PWA — Service Worker

`sw.js` stale-while-revalidate стратегиясини ишлатади:
- **Install**: HTML, иккала JSON, икона, manifest ни кэшга юклайди.
- **Fetch**: same-origin GET — кэшдан зудлик билан жавоб + фонда янги версияни юклаб қўяди.
- **Cross-origin** (Google Fonts) — браузерга ўтказиб юборади.

`index.html` SW регистрацияси + `updatefound` тингловчи: янги SW тайёр бўлганда `.update-banner` чиқарилади. Фойдаланувчи "Янгилаш" босгач, SW га `skip-waiting` хабари юборилади ва `controllerchange` дан кейин саҳифа янгиланади. Dismiss тугмаси `localStorage` да билд id ни сақлайди — шу версия учун қайта кўрсатилмайди.

**Янгилаш оқими**:
1. `data.json` таҳрирлайман → `python3 build_index.py` → push.
2. Build sw.js cache id ни автоматик бумп қилади.
3. Фойдаланувчи сайтни очса (ёки PWA сифатида қайта очса), браузер янги sw.js ни кўради → орқа фонда юклайди → банер чиқади.

### Янги функция қўшиш йўриғи

- Янги саҳифа: HTML'да янги `<div id="pgXxx" class="page">` қўшиш, `showPage()`'нинг массивига киритиш, App.openXxx() метод яратиш, `applyHash()`'да URL pattern қўшиш.
- Янги қидирув майдони: `doSearch()` ичида `inX = ...includes(q)` қўшиш, `_renderSearch()`'да label ўзгартириш.
- Янги breadcrumb элементи: керакли open* методда `bc(...)` чақирувида item қўшиш.

## Workflow

1. Фойдаланувчи бир неча боб/ҳадис учун манба расмларини юборади (одатда 2-3 боб бирга).
2. `data.json`-нинг шу қисмини ўқиб, манба билан солиштираман.
3. Фарқли жойларни Python скрипти билан янгилайман.
4. Verify қилиб, `git diff --stat` бир сатрлик ўзгаришни кўрсатиши керак.
5. Commit + push `main` га (GitHub Pages 1-2 дақиқада янгилайди).

### Commit message услуби

```
Update X-kitob A-B-bob nomi/muallaqot, N-M-hadis matn/rowi/izoh from source

- A-bob: nomi (қисқача нима ўзгарди)
- N-hadis: matn aniqlandi (...)
- ...

https://claude.ai/code/session_<id>
```

## Прогрессни топиш

Қаерда тўхтаганлигимизни топиш:
```bash
git log --oneline | head -5
```

Сўнгги commit'ни ўқиб, охирги таҳрирланган боб/ҳадисни аниқлаш мумкин.

## Тарих (асосий босқичлар)

### Контент
- 6-китоб (Ҳайз китоби) — 1-30-боб, 294-333-ҳадис **тўлиқ таҳрирланди**
- 7-китоб (Таяммум китоби) — 1-3-боб, 334-337-ҳадис таҳрирланди

### Маълумот сифати (батч аудит ва тузатишлар)
- **Боб номлари** — 1821 та номни `N-БОБ. <Сарлавҳа>` форматига меъёрлаштириш (`1-боб.`, `4 БОБ.`, `11- БОБ.` каби 17 вариант бирлаштирилди)
- **Rowi (ровий)** — 46 та тузатиш: қўш бўш жой, кичик ҳарф бошланиш, олд ҳадис рақамлари (`3093. Оиша` → `Оиша`), бир имло (`розияллоуу` → `розияллоҳу`)
- **Izoh (изоҳ)** — 28 та тузатиш: қўш бўш жой, кичик ҳарф, таҳрирчи белгиси (`???`) олиб ташланди
- **Ҳадис матни (matn)** — 815 та ҳадис, 2277 та форматлаш тузатиши: қўш бўш жой (626), вергул+ҳарфдан кейин бўш жой (496), нуқта+ҳарфдан кейин бўш жой (630), тиниш белгиси олдида ортиқча бўш жой (525)
- **Латин ҳарфлар (OCR хатоси)** — 69 та ҳадисда 3103 та латин ҳарф Кириллга ўгирилди (`a→а`, `o→о`, `e→е` ва ҳ.к.). Қолганлар: `Uromastyx aegyptia` (илмий ном — қасддан), k65b171h4698 даги `манfбу` (қўлда текшириш керак)
- **Қўштирноқлар** — 4781 та қўштирноқ Кирилл `«»` га ўгирилди: типографик `"`/`"` ва тўғри `"` контекстга қараб
- **Дубликатлар** — 2 та аниқ дубликат ҳадис олиб ташланди (`k10b128h799`, `k64b9h3984`). 72 та қолган муаммо `data_issues.csv` га экспорт қилинди (gitignore — қўлда текшириш керак)

### Таржимасиз 272 ҳадисни тўлдириш (2026-06-02 → 2026-06-03)
- **Манба 1 — DOCX (`buxoriy kitoblari/*.docx`)**: `parse_docx_silsila.py` v3 ўзгариш билан 232 та ҳадис топилди:
  - Жуфтлик гуруҳ форматлари: `408, 409.`, `1055-1056-`, `1156-1157-1158.`, `1997. 1998.`
  - Closure-асосли матн ажратиш (`»...дедилар.` дан кейин гуруҳ бошланиши)
  - Multi-hadis пайдар-пайлари бир хил матн оладиган (parallel narration)
  - Жуфт ID да биринчи ID дан нусха кўчириш
- **Манба 2 — Android app (Оltin Silsila)**: docx'да йўқ 33 та ҳадис **`apply_pair_copies.py`** билан тўлдирилди (предшественникдан копия — 43 та намуна app'да тасдиқланди).
- **Қўлдан**: h6154, h6155 (app'да ҳам йўқ) арабчадан таржима қилинди; h1428 (app screenshot'дан).
- **Менинг пилотларим**: h239, h534, h537, h897 — кейин h239 ва h897 ни app версиясига алмаштириб қўйилди (h534, h537 аллақачон айнан мос эди).
- **Якуний**: 7,286 → **7,558 та таржимали ҳадис** (+ 272 новый). Жами `tarjima_status='missing'` тугатилди.

### Метадата тозалаш (2026-06-03)
- `data.json` дан кераксиз inline `matn_ar` (272 та) олиб ташланди — `arabic.json` да аллақачон бор. `tarjima_status` майдонлари ҳам тозалаб қўйилди (фақат `missing` керакли эди, у кетган).
- `data.json`: 11.42 → 10.91 МБ (-540 КБ).
- `arabic.json` га 66 та етишмаган арабча `fill_missing_arabic.py` орқали қўшилди (skeleton-mapping ишончсиз деб ташлаб қўйганлари; `hadithnumber` бўйича `ar_source.json` дан).
- 4 та қолган арабчасиз ҳадис (5712, 5774, 5775, 6175) — канон Бухорийда range-формат: бир ҳадис, бир неча ID. `fill_range_arabic.py` уларни range-leader (5709, 5773, 6173) дан нусхалади (sunnah.com'дан текширилган). `arabic.json`: 7,488 → **7,558 (100% қамраш)**.
- **5 та canon ID data.json'да йўқ** (5710, 5711, 6074, 6075, 6174) — Мансур таржимасида қасддан ўтказиб юборилган (улар канон диапазон-ҳадисларининг "alternate chain" нусхалари). Аудитда "Gaps: 5" сифатида кўринади, лекин амалда муаммо эмас.
- Strays: 9 → 0 (китоб-merge ишларидан кейин канон билан мослашган).
- Duplicates: 15 → 0 (Step 6'да ҳал бўлган).
- **`index.html`да дед код**: `tarjima_status === 'missing'` бранчлар (қаторлар 840-844, 894-902) — `undefined === 'missing'` = false → яхши таржима кўрсатиш йўли бажарилади. Қолдирилган.

### Инфратузилма
- Икки иконка стилистик ўзгаришлари: `apple-touch-icon.png` FreeSerif Bold Italic шрифтида қайта яратилди
- Уч босқичли юклаш: инлайн BOOKS → `index.json` → `data.json`
- **PWA**: Service Worker, офлайн режим, "🔄 Янги версия" банер
- **Lazy-render**: `content-visibility:auto` билан кўринмаган карталар рендердан четлатилади

## Чат лимити (~32 МБ)

Чат ҳажми 32 МБ дан ошса янги чат очиш керак. Шунинг учун:
- **Имкон бўлса манба матнини расм эмас, **матн** шаклида юборинг** — 50-100 марта тежайди.
- Янги чат очганда: ушбу `CLAUDE.md`-ни ўқиб, `git log --oneline | head` дан охирги тўхтаган жойни топиб, давом этасиз.
